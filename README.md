# keyboard-layout-fix
Browser extension that converts text typed in the wrong keyboard layout. It works in input, textarea, and most custom fields. You can convert selected or full text. Includes automatic correction with context menu controls and per-site exclusions. Built with Manifest V3.
